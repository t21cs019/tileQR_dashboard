# バージョン管理方針

本リポジトリは [セマンティックバージョニング (SemVer)](https://semver.org/lang/ja/) に従う。

## 形式: `MAJOR.MINOR.PATCH`

| 部分 | 上げるとき | 例 |
|------|-----------|-----|
| MAJOR | 互換性を壊す変更 / 正式版の節目 | `1.0.0` |
| MINOR | 後方互換な機能追加（グラフ追加など） | `0.2.0` |
| PATCH | バグ修正・小調整 | `0.2.1` |

- `0.x.y` は開発版。`1.0.0` で正式版とする。
- リリースごとに `pyproject.toml` の `version` をタグと揃える。

## リリース履歴

| バージョン | 内容 |
|-----------|------|
| `v0.1.0` | 初版（集約・可視化・Webダッシュボード） |
| `v0.2.0` | nb×GFlops 折れ線グラフ（CPU比較）を追加 |
| `v0.3.0` | CPUプリセットによる取り込み時のCPU情報の自動補完 |
| `v0.4.0` | 識別単位を host → (label, threads, size) に変更。複数size対応・host横断の平均集約 |
| `v0.5.0` | nb-GFlops曲線の組複数選択・散布図の軸変更(nb×スレッドあたりキャッシュ)・PNGエクスポート |

## タグの打ち方（例）

    git tag -a v0.2.0 -m "nb×GFlops 折れ線グラフ追加"
    git push origin v0.2.0

GitHub の Releases でタグに対してリリースノートを書くと、変更点が一覧で残って見やすい。